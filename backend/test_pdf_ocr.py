import os
import fitz
from dotenv import load_dotenv

load_dotenv()
from notes.utils import extract_text_from_image

def test_pdf_ocr():
    # create a dummy pdf with an image or just test the logic
    pass
